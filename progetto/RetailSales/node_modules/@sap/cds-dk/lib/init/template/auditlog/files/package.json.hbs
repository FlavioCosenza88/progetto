{
  {{#cap.isNodejs}}
  "dependencies": {
      "@sap/audit-logging": "^5"
  },
  "cds": {
  {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
      {{/cap.for}}
      {{#cap.shortcut}}
        "audit-log": "service"
      {{/cap.shortcut}}
      {{^cap.shortcut}}
        "audit-log": {
          "kind": "service"
        }
      {{/cap.shortcut}}
      {{#cap.for}}
      }
      {{/cap.for}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
