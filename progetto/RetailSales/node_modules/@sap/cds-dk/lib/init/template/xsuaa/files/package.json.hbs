{
  {{#cap.isNodejs}}
  "dependencies": {
    "@sap/xssec": "^3",
    "passport": "^0"
  },
  "cds": {
  {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
      {{/cap.for}}
      {{#cap.shortcut}}
        "auth": "xsuaa"
      {{/cap.shortcut}}
      {{^cap.shortcut}}
        "auth": {
          "kind": "xsuaa"
        }
      {{/cap.shortcut}}
      {{#cap.for}}
      }
      {{/cap.for}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
