{
  {{#cap.isNodejs}}
  "dependencies": {
    "@sap/xb-msg-amqp-v100": "^0"
  },
  "cds": {
  {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
      {{/cap.for}}
        "messaging": {
          "kind": "enterprise-messaging-shared"
        }
      {{#cap.for}}
      }
      {{/cap.for}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
