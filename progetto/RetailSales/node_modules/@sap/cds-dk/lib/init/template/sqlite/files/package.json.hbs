{
  {{#cap.isNodejs}}
  "devDependencies": {
    "sqlite3": "^5"
  },
  "cds": {
  {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
      {{/cap.for}}
      {{#cap.shortcut}}
        "db": "sqlite"
      {{/cap.shortcut}}
      {{^cap.shortcut}}
        "db": {
          "kind": "sqlite"
        }
      {{/cap.shortcut}}
      {{#cap.for}}
      }
      {{/cap.for}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
