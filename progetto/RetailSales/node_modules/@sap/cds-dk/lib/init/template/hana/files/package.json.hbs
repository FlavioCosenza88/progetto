{
  {{#cap.isNodejs}}
  "dependencies": {
      "hdb": "^0.19.0"
  },
  "cds": {
  {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
        {{#cap.shortcut}}
        "db": "hana-cloud"
        {{/cap.shortcut}}
        {{^cap.shortcut}}
        "db": {
          "kind": "hana-cloud"
        }
        {{/cap.shortcut}}
      },
      {{/cap.for}}
      {{#cap.shortcut}}
      "db": "{{^cap.for}}hana-cloud{{/cap.for}}{{#cap.for}}sql{{/cap.for}}"
      {{/cap.shortcut}}
      {{^cap.shortcut}}
      "db": {
        "kind": "sql"
      }
      {{/cap.shortcut}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
