{
  {{#cap.isNodejs}}
  "dependencies": {
    "@sap/cds-mtxs": "^1"
  },
  "cds": {
  {{/cap.isNodejs}}
    {{#cap.isJava}}
    "profiles": ["with-mtx-sidecar", "java"],
    "build": {
      "target": "."
    },
    {{/cap.isJava}}
    {{#cap.isNodejs}}
    "profile": "with-mtx-sidecar",
    {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
      {{/cap.for}}
        "multitenancy": true
      {{#cap.for}}
      }
      {{/cap.for}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
