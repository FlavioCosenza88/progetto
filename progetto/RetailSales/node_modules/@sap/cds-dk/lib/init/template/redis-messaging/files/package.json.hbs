{
  {{#cap.isNodejs}}
  "dependencies": {
    "redis": "^4"
  },
  "cds": {
  {{/cap.isNodejs}}
    "requires": {
      {{#cap.for}}
      "[{{cap.for}}]": {
      {{/cap.for}}
        "messaging": {
          "kind": "redis-messaging"
        }
      {{#cap.for}}
      }
      {{/cap.for}}
    }
  {{#cap.isNodejs}}
  }
  {{/cap.isNodejs}}
}
